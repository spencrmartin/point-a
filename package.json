{
  "name": "point-a",
  "version": "0.1.0",
  "private": true,
  "description": "A local-first issue tracker with MCP integrations",
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "lint": "turbo lint",
    "db:migrate": "pnpm --filter @point-a/api db:migrate",
    "db:studio": "pnpm --filter @point-a/api db:studio"
  },
  "devDependencies": {
    "turbo": "^2.3.0",
    "typescript": "^5.7.0"
  },
  "packageManager": "pnpm@9.12.3",
  "engines": {
    "node": ">=20"
  }
}
